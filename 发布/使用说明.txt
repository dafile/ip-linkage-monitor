﻿掌上看家联动 IP 在线状态监控 v1.4.0 —— 使用说明
================================================

【两种成品，任选其一】
1. ip-linkage-monitor-v1.4.0.exe（约 10 MB）
   绿色单文件版：双击即用，无需安装。
   配置和日志保存在本文件所在的 data\ 文件夹，删除 data\ 即完全重置。
   （v1.4.0 修复：配对缓存设备不再被误判为在线）

【环境要求】
Windows 10/11 x64，系统需有 WebView2 运行时（一般已内置）。
蓝牙监控：推荐先将手机与电脑在系统蓝牙中配对一次，程序会通过系统 API
维持低功耗蓝牙连接——手机在蓝牙范围内即在线，离开范围即离线。
需要本机有蓝牙适配器。

【快速上手】
1. 首次启动会自动从注册表读取掌上看家采集端安装路径。
2. 「设置 → 监控方式」选择 IP Ping 或 蓝牙邻近 监控（二选一）。
   - IP 模式：填写要监控的 IP（默认 172.30.109.238）。
   - 蓝牙模式：填写设备名称或 MAC，建议用「扫描拾取」选择。
3. 「设置 → 联动规则」配置规则：
   触发时机（目标上线/离线）× 延迟秒数 × 动作 × 备注。
   动作可选：启动 / 彻底关闭监控程序、显示 / 隐藏窗口、
   自定义执行脚本或程序（.bat / .cmd / .ps1 / .exe）。
   每条规则可「试运行」调试。
4. 回到「控制台」打开联动总开关，开始自动运行。
5. 关闭窗口默认最小化到托盘，程序后台继续运行；
   真正退出请用托盘右键 → 退出程序（可在设置中改为关闭即退出）。

【重要：蓝牙可见性说明】
经典蓝牙扫描只能发现：① 与电脑已配对且保持蓝牙连接的设备；
② 处于「可被发现」状态的设备（如手机停在蓝牙设置页）。
多数手机平时息屏时不可被蓝牙发现，这是手机系统限制。
要实现「手机在附近就算在线」，请让手机与电脑保持蓝牙连接
（如连接电脑的蓝牙音频），或选择手环/手表等持续广播的设备。

【数据位置（绿色版）】
  data\config.json      全部设置与联动规则（可直接编辑）
  data\logs\log-*.jsonl 按天日志（界面可筛选/导出 CSV/清空）

【开源地址】
https://github.com/dafile/ip-linkage-monitor
